class UserArguments {
  final bool isLurah;
  final bool isNakes;
  final String username;
  final String email;
  final String password;
  final bool isLoggedIn;
  UserArguments(this.isLurah, this.isNakes, this.username, this.email,
      this.password, this.isLoggedIn);
}
